#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - is the entry point
 * Description:is everything
 * Return:success or failer 0
 */
int main(void)
{
int g;
char low;
for (g = '0'; g <= '9'; g++)
putchar(g);
for (low = 'a'; low <= 'f'; low++)
putchar(low);
putchar('\n')
return (0);
}
