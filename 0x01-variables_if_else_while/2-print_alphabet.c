#include <stdio.h>

#include <stdlib.h>

#include <time.h>
/**
 * main - is the entry point
 * Description: determine code info
 * Return: success 0
 */
int main(void)
{
char low;
for (low = 'a'; low <= 'z'; low++)
putchar(low);
putchar('\n');
return (0);
}
