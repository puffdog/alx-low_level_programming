#include "main.h"
/**
 *_abs - is the main entry
 *@a: is the number value
 *Description: This function returns absolute value of a number
 *Return: return number of a
 */
int _abs(int a)

{

if (a >= 0)
return (a);
else
return (a * -1);
}
